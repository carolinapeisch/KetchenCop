<div class="container">

  <%= render 'header' %>
  <span class="label label-danger">Edit</span>
  <p id="notice"><%= notice %></p>
  
  <%= render 'form' %>
  
  <%= link_to 'Show', @image, :class => 'btn btn-primary spacer' %>
  <%= link_to 'Back', images_path, :class => 'btn btn-primary spacer' %>

</div>
